# Agent Activity Dock

这是一个面向本地 Agent 用户的低干扰状态提示层。它只告诉用户 Agent 是否仍在工作，以及是否出现了值得回来看看的新情况。

## Language

**Agent**：执行自动化任务、并能向 Dock 发出状态事件的外部工具，例如 Codex、Claude 或 DSH。

**Dock**：独立运行的本地状态提示层；它接收 Agent 的明确事件，但不读取 Agent 内容、不控制 Agent，也不通过扫描进程推断状态。

**小球**：Dock 的收缩呈现。它是一个尽量不占空间的独立小窗口，用户可以点击或用热键重新查看相关终端。
_避免_：桌宠、像素办公室、完整桌面壳

**工作中**：Agent 正在执行任务，用户通常不需要立即介入。

**不在工作**：Agent 已停止持续执行；可能是正常结束、出错、等待用户输入，或出现了需要用户回看的新情况。小球不要求进一步区分这些原因。

**提示**：Agent 从“工作中”切换后，Dock 对用户发出一次可感知的提醒，让用户知道应该回来查看。
_避免_：通知轰炸、持续打扰

**状态事件**：Agent 主动发送给 Dock 的一次状态变化消息，例如开始工作、正常结束、报错或等待用户输入。Dock 不通过扫描终端或进程来生成状态事件。

**手动重置**：用户在状态事件丢失、强制终止或断电等极端情况下，将小球从“工作中”恢复为“不在工作”的显式操作。

**聚合小球**：多个 Agent 共用的一个小球。只要有一个被追踪的 Agent 正在工作，小球就表示工作中；小球上的数字显示工作数量与被追踪数量。

**工作数量**：当前仍处于“工作中”的被追踪会话数量。它不是已安装的 Agent 类型数量。

**追踪数量**：Dock 当前知道并展示的任务总数。聚合小球用 `工作数量/追踪数量` 表示整体负载，例如 `2/3`。

**待查看标记**：工作停止后留下的小型 `!` 标记，表示用户还没有查看这次结束、报错或等待输入。查看小球或对应任务后清除；它不是第三种工作状态。
