[package]
name = "agent-activity-dock-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[[bin]]
name = "dock"
path = "src/main.rs"

[dependencies]
agent-activity-dock-adapters = { path = "../dock-adapters" }
agent-activity-dock-connect = { path = "../dock-connect" }
agent-activity-dock-core = { path = "../dock-core" }
agent-activity-dock-ipc = { path = "../dock-ipc" }
agent-activity-dock-service = { path = "../dock-service" }
clap = { version = "4", features = ["derive"] }
serde_json.workspace = true
time.workspace = true
