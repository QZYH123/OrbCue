# Aggregate Tracked Agents into One Ball

The Dock will present one aggregate ball instead of one floating window per Agent. Its border color communicates whether any tracked session is working, and a compact count shows working sessions over tracked sessions. This keeps the overlay from multiplying as users run more Agents while preserving a quick workload signal; a click can open the per-session list when detail is needed.
