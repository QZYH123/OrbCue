# 10 — 会话卡片显示项目路径并按项目分节

**What to build:** 多开 Agent 时能看出每条记录属于哪个项目：卡片显示短路径，列表按项目分节排序。小球仍只显示 `工作数量/打开数量`。

**Blocked by:** 无（03 已完成）

**Status:** complete

- [x] 路径只来自明确事件字段（如 Grok 的 `cwd` / `workspaceRoot`），不读磁盘、`.git` 或进程 cwd 去猜。
- [x] 卡片显示缩短路径（`~` 折叠、过长时中间省略）；无路径的会话正常展示，不显示假路径。
- [x] 列表按项目分节排序，同项目会话相邻并带一行小节头；无路径会话归「其他」一节。不做折叠交互。
- [x] 小球计数、角标优先级、`pending = 打开总数 − working` 全部不变。
- [x] 行为测试：有路径分节、无路径不崩溃、快照不写入摘要或 transcript。
