# 04 — 添加一次性提示和待查看标记

**What to build:** 当任务从工作中停止、等待输入或报错时，用户收到一次短音和短闪；小球保留 `!`，直到用户查看或清除，重复事件不重复骚扰。

**Blocked by:** 03 — 显示聚合悬浮小球

**Status:** complete

- [x] 正常停止、等待输入和报错都能触发一次提示。
- [x] 同一任务的重复事件不会再次播放或闪动。
- [x] `!` 在用户查看对应任务前保持可见，查看后清除。
- [x] 声音播放失败时状态和 `!` 仍然提交，Agent 不受影响。
- [x] 提示不是常驻动画，也不会在普通工作事件中触发。
- [x] 用 fake sound sink 测试外部效果，不 mock 自己的状态模块。
